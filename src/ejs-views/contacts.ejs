<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
    <body>
        <%- include('./partials/nav.ejs') %>
        <h1>Contacts:</h1>
        <ul>
            <% if (contacts.length) { %> <% contacts.forEach(({ link, service }) => { %>
            <li>
                <a href="<%= link %>"><%= service%></a>
            </li>
            <% }) %> <% } %>
        </ul>
    </body>
</html>
